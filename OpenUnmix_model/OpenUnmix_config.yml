model: 
  name: OpenUnmix
# Training config
training:
  epochs: 100
  batch_size: 2
  num_workers: 24
  half_lr: yes
  early_stop: yes
# Optim config
stft:
  nb_bins: 1026
  window_size: 1024
  hop_size: 32
optim:
  optimizer: adam
  lr: 0.001
  weight_decay: !!float 1e-5
# Data config
data:
  train_dir: podcastmix/metadata/train
  valid_dir: podcastmix/metadata/val
  sample_rate: 8000
  n_src: 2
  segment: 2
